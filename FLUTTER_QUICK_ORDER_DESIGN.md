# Flutter — طلب سريع (Custom Order Requests)

مواصفات تصميم وتنفيذ لتطبيق الموبايل، متوافقة مع تصميم موقع المستخدم (Tikmart Web) مع تكييف لشاشات أصغر وفلاتر.

---

## 1. الهدف

نفس فلو الويب: الزبون يكتب طلبه (متل لستة السوق) + صور اختيارية → الإدارة تسعّر → الزبون يوافق أو يلغي → ينشأ طلب نظامي.

**الفرق عن الويب:** مساحة أقل، إيماءات لمس، هيدر/تبويبات تطبيق، لا سكشن عريض مثل الهوم ويب — لكن **نفس الهوية البصرية واللغة**.

---

## 2. الهوية البصرية (مطابقة الويب)

### الألوان

| Token | قيمة مقترحة | استخدام |
|--------|-------------|---------|
| `primary` | من ثيم التطبيق (ويب: برتقالي/سيان Tikmart) | أزرار أساسية، شارة مستعجل، أيقونة البرق |
| `primaryLight` | نسخة أفتح من primary | نصوص مساعدة، حدود ناعمة |
| `expressWash` | `#FFF7F0` → `#FFE8D6` | خلفية بانر الدعوة (فاتح) |
| `expressWashDark` | `#1A1410` → `#2E1C12` | نفس البانر في الوضع الداكن |
| `surface` | أبيض / سطح الكارد | بطاقات المراحل، قائمة الطلبات |
| `textPrimary` | `#0F172A` / أبيض في داكن | عناوين |
| `textSecondary` | `#64748B` / أبيض 75% | هينت ووصف |
| `success` | أخضر النظام | حالة `approved` |
| `warning` | كهرماني | `pending_pricing` / `waiting_approval` |
| `danger` | أحمر النظام | إلغاء / ملغى |

> لا تستخدم تدرج بنفسجي أو خلفية كوبون خصم برتقالية صاخبة — بانر الطلب المستعجل **دافئ كريمي + primary**، مختلف عن بانرات العروض.

### الطباعة

- عناوين: **Bold / ExtraBold**، حجم أكبر بقليل من باقي الهوم (مثلاً 22–26 sp على الموبايل).
- جسم النص: Regular 14–15 sp، ارتفاع سطر مريح للقراءة بالعامية.
- شارات الحالة: 11–12 sp SemiBold.
- RTL افتراضي للعربية؛ LTR للإنجليزي.

### الشكل

- زوايا كبيرة: `20–28` للبانر، `16–20` للكروت، `999` (full) لأزرار الـ CTA الرئيسية.
- ظل خفيف دافئ تحت الكروت (ليس multi-layer ثقيل).
- أيقونة مميزة: **برق (Zap / Lightning)** لكل دخول لطلب سريع.

---

## 3. أين يظهر في التطبيق؟

| مكان | وصف Flutter | ملاحظة شاشة صغيرة |
|------|-------------|-------------------|
| **هوم** | بطاقة/بانر أعلى المحتوى (تحت الهيدر أو أول سلايدر) | عمود واحد؛ المراحل **أفقية قابلة للسحب** أو **صف مضغوط** |
| **هيدر / AppBar** | زر واحد فقط «طلب سريع» بجانب الأيقونات (سلة/حساب) — **لا تكرّر** في تبويب سفلي أو قائمة روابط | أيقونة برق داخل دائرة primary + نص «مستعجل / طلب سريع» في سطرين صغيرين إن ضاق العرض |
| **قائمة الحساب** | بند «طلبات سريعة» | أيقونة Zap |
| **إشعار** | `type: custom_order_request` → يفتح تفاصيل الطلب | Deep link: `/custom-orders/{id}` |

**قاعدة:** زر نافبار واحد فقط (جنب الأيقونات). السكشن على الهوم هو اللي يجذب؛ الزر للوصول السريع.

---

## 4. الشاشات

### 4.1 بانر الهوم (دعوة)

```
┌─────────────────────────────────────┐
│  [⚡ طلب مستعجل]                    │
│  بدك شي عالسريع؟                    │
│  اكتبه متل لستة السوق…             │
│                                     │
│  ┌────┐ ┌────┐ ┌────┐  أو Scroll   │
│  │ 1  │ │ 2  │ │ 3  │              │
│  └────┘ └────┘ └────┘              │
│                                     │
│         [ ⚡ اطلب هلق  ← ]          │
└─────────────────────────────────────┘
```

**محتوى (عامية سورية — نفس الويب):**

| مفتاح | نص AR |
|--------|--------|
| kicker | طلب مستعجل |
| headline | بدك شي عالسريع؟ |
| hint | اكتبه متل لستة السوق. نحنا منسعره، بتوافق عليه، ومنوصله لعند باب البيت. |
| cta | اطلب هلق |
| step 1 | اكتب طلبك — بكلامك العادي |
| step 2 | منسعرلك — بتوافق قبل الدفع |
| step 3 | منوصله — لعند باب البيت |

**تكييف موبايل:**

- البانر بعرض الشاشة مع padding أفقي 16.
- الخطوات: `ListView` أفقي ببطاقات بعرض ~42% من الشاشة، أو عمود من 3 صفوف إن فضّلت بدون سكرول.
- زر CTA بعرض كامل تحت الخطوات على الموبايل (أسهل لمس).
- لا تضع البانر فوق هيرو ضخم يتزاحم معه في أول viewport — إمّا بعده مباشرة أو بديله إذا الهيرو مزدحم.

---

### 4.2 إنشاء طلب (`CustomOrderCreatePage`)

حقول (نفس API الويب):

1. **الوصف** (إجباري) — `TextField` متعدد الأسطر، `minLength: 10`، عدّاد أحرف.
2. **صور** (اختياري، حد 5) — شبكة معاينة + `image_picker` / كاميرا؛ مفاتيح `images[]`.
3. **العنوان** — اختيار من عناوين المستخدم (نفس selector التطبيق).
4. **طريقة الدفع** — نية فقط؛ ملاحظة: «ما بينحسب عليك قبل ما توافق على السعر».
5. **وقت متوقع** — اختياري (`expected_at` ISO).

**تخطيط موبايل:**

- نموذج عمودي داخل `SingleChildScrollView`.
- زر الإرسال ثابت أسفل الشاشة (`SafeArea` + sticky bottom bar) مع ظل خفيف.
- معاينة الصور: شبكة 3 أعمدة، مربع 1:1، زر × للإزالة.
- عند الخطأ من السيرفر: SnackBar / Banner؛ لا تعتمد على التحقق المحلي وحده.

**بعد النجاح:** `pushReplacement` إلى شاشة التفاصيل `id` الناتج.

---

### 4.3 قائمة الطلبات (`CustomOrderListPage`)

- فلتر شيبس أفقية: الكل / قيد التسعير / بانتظار الموافقة / موافق / ملغى.
- كرت لكل طلب: صورة أولى (أو أيقونة برق)، شارة حالة، مقتطف وصف (سطران)، تاريخ.
- فارغ: رسم بسيط + «اطلب هلق».
- سحب للتحديث + ترقيم لا نهائي إن وُجد `meta`.

---

### 4.4 تفاصيل الطلب (`CustomOrderDetailPage`)

حسب `status` + `actions.can_approve` / `can_cancel`:

| Status | العرض |
|--------|--------|
| `pending_pricing` | وصف + صور + تنبيه «قيد مراجعة الإدارة» + إلغاء إن `can_cancel` |
| `waiting_approval` | جدول/قائمة بنود التسعير + مجموع + تنبيه بنود خارجية + موافقة / إلغاء |
| `approved` | رسالة نجاح + زر تتبع الطلب النظامي `order_id` |
| `cancelled` / `cancelled_by_admin` | رسالة نهائية + `rejection_reason` إن وُجد |

**بنود التسعير (موبايل):**

- لا جدول عريض — **قائمة كروت**: اسم المنتج، كمية، سعر الوحدة، الإجمالي، شارة «خارجي»، رابط فاتورة إن وُجد.
- أسفل القائمة: ملخص (فرعي / توصيل / إجمالي) في كرت ثابت أو قسم ملخص.

**تنبيه التفاوت (بنود خارجية):**

```
الأسعار النهائية للبنود الخارجية قد تختلف بحدود ±X% (أو ±قيمة)
حسب التوفر في السوق.
```

**بعد الموافقة:** انتقل لتتبع الطلب العادي (`GET /user/orders/{order_id}` أو شاشة التتبع الموجودة).

---

## 5. API (نفس الويب)

**Base:** `/api/user`  
**Auth:** Bearer `auth:user`  
**إنشاء مع صور:** `multipart/form-data`

| Method | Path |
|--------|------|
| `POST` | `/custom-order-requests` |
| `GET` | `/custom-order-requests` (`?status=`) |
| `GET` | `/custom-order-requests/{id}` |
| `POST` | `/custom-order-requests/{id}/approve` |
| `POST` | `/custom-order-requests/{id}/cancel` |

**إنشاء — FormData:**

```
description          // ≥ 10
address_id
payment_method_id?   // نية دفع فقط
expected_at?         // ISO
images[]             // ملفات
```

الحالة الابتدائية: `pending_pricing`. لا تسعير عند الإرسال.

**من التفاصيل — `data.order`:**

- `items[]`: `product_name`, `quantity`, أسعار مزدوجة إن وُجدت، `is_external`, `invoice_image`
- `total`, `delivery_price`, `subtotal`
- `has_external_items`, `price_variance_type`, `price_variance_value`
- `approximate_total` اختياري

**أزرار:** فقط عبر `data.actions.can_approve` و `can_cancel`.

**دفع:** `is_paid` يبقى `false` حتى `approve`؛ النقد عند الاستلام حسب منطق البوابة الحالي.

---

## 6. مخطط الحالات

```
pending_pricing  →  waiting_approval  →  approved
        \                 /
         → cancelled / cancelled_by_admin
```

| Status | لون الشارة (مقترح) |
|--------|---------------------|
| `pending_pricing` | warning / كهرماني |
| `waiting_approval` | primary فاتح |
| `approved` | success |
| `cancelled*` | danger |

---

## 7. إشعارات

- Push / inbox: `type: custom_order_request` + غالباً `waiting_approval` → افتح تفاصيل الطلب مع CTA موافقة بارز.
- بعد الموافقة: إشعارات الطلب العادي `type: order`.

---

## 8. مكوّنات Flutter مقترحة

```
lib/features/custom_order/
  data/
    custom_order_api.dart
    custom_order_models.dart
  presentation/
    pages/
      custom_order_list_page.dart
      custom_order_create_page.dart
      custom_order_detail_page.dart
    widgets/
      quick_order_home_banner.dart
      quick_order_app_bar_button.dart
      custom_order_status_chip.dart
      custom_order_image_picker.dart
      custom_order_pricing_list.dart
      price_variance_alert.dart
```

**Widgets مشتركة مع الويب مفاهيمياً:**

- `QuickOrderHomeBanner`
- `QuickOrderAppBarButton` (الوحيد في الهيدر)
- `CustomOrderStatusChip`
- `PriceVarianceAlert`

---

## 9. قواعد شاشة صغيرة (إلزامي)

1. **عمود واحد** لكل الشاشات؛ لا صفّان متوازيان للمحتوى الرئيسي.
2. **CTA بعرض كامل** في الأسفل على الإنشاء والموافقة.
3. **لمس ≥ 44×44** للأزرار والأيقونات القابلة للنقر.
4. **لا تكرار** لزر الطلب السريع في أكثر من مكان بالهيدر.
5. **الصور:** ضغط قبل الرفع إن أمكن؛ حد 5؛ معاينة فورية.
6. **الكيبورد:** `resizeToAvoidBottomInset`؛ حقل الوصف لا يُغطّى بزر الإرسال.
7. **الوضع الداكن:** نفس تدرجات `expressWashDark` المستخدمة على الويب.
8. **الحركة:** خفيفة — ظهور البانر، نبضة خفيفة لزر الهيدر (مرة أو مرتين عند أول فتح)، بدون ضوضاء.

---

## 10. نسخ الواجهة (مختصر)

### AR (عامية سورية)

- هوم CTA: اطلب هلق  
- نافبار: مستعجل / طلب سريع  
- هينت نافبار: اكتب اللي بدك ياه — منحسبه ومنوصّله.  
- قائمة فارغة: لسا ما في طلبات سريعة — اطلب هلق  

### EN

- Need it now? / Order now / Urgent · Quick order  
- Write what you need — we price it and deliver.

---

## 11. قبول التصميم (Checklist)

- [ ] بانر الهوم يطابق روح ألوان الويب (كريمي دافئ + primary) وليس كوبون خصم.
- [ ] زر هيدر واحد جنب الأيقونات، مميّز وغير مكرر.
- [ ] إنشاء: وصف ≥ 10، صور ≤ 5 مع معاينة، عنوان، دفع كنية.
- [ ] تفاصيل: حالات الأربع واضحة؛ أزرار حسب `actions`.
- [ ] موافقة → شاشة تتبع الطلب النظامي.
- [ ] إشعار `custom_order_request` يفتح التفاصيل.
- [ ] RTL سليم؛ نصوص لمس مريحة على 375px عرضاً.

---

## 12. مرجع الويب

تنفيذ الموقع الحالي:

- بانر: `src/features/home/components/QuickOrderHomeBanner.tsx`
- زر نافبار: `src/components/Navbar.tsx` (صف الأيقونات العلوي فقط)
- فلو الصفحات: `src/features/customOrder/`
- ترجمات: `src/locales/ar.json` → `customOrder` / `navbar.quickOrder*`
- مسارات API: `src/utils/apiRoutes.ts` → `customOrderRequests`

استخدم هذا الملف كمصدر حقيقة لفريق Flutter؛ أي اختلاف بصري مقصود بسبب الموبايل يُوثَّق هنا وليس عشوائياً.
