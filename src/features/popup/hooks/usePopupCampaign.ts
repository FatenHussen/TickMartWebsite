/**
 * Convenience hook for components that need to interact with the active popup.
 * All state is owned by PopupProvider — this is a thin selector.
 */
export { usePopupContext as usePopupCampaign } from "../providers/PopupProvider";
