/**
 * Brand-tinted focus rings only (no default browser outline / no black stroke).
 * Use with `outline-none`.
 */
export const HELP_FOCUS_RING =
    "outline-none focus-visible:ring-2 focus-visible:ring-primary/40 focus-visible:ring-offset-0";

/** For text fields: show ring on any focus (mouse + keyboard). */
export const HELP_FOCUS_RING_INPUT =
    "outline-none focus:ring-2 focus:ring-primary/35 focus:ring-offset-0";
